# EEG analysis pipeline dependencies.
# The core pipeline runs on these (already present in the project .venv):
mne>=1.6
scikit-learn>=1.3
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
pandas>=2.0
pyyaml>=6.0
joblib>=1.3

# --- Optional enhancers (the pipeline auto-detects them and degrades cleanly) ---
# python-picard   # faster/robuster ICA (preprocess.ica.method: picard)
# mne-icalabel    # automatic IC classification (preprocess.ica.use_iclabel: true)
# autoreject      # data-driven epoch rejection (not required; MNE p2p used instead)
